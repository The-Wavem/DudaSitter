// Legacy reference.
// The old localStorage mock helpers were replaced by the Firestore services in src/services/.
// Keep this file empty of runtime exports so new code does not depend on the mock layer.
